const daily = {  //this creates the object
    
    
    
    convertToFahrenheit: (tempCelcius) => { //this is the stub function, useful as it is a guide to how it should work
        
        return ((tempCelcius*1.8)+32);
        // when writing minimum amount of code, can just use return the expected value
        //lookup and practice edge cases
    }
};

export default daily;